{
  "name": "mytask-cf-repo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "cloudflare"
  ],
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "npm --workspace cloudflare run typecheck"
  },
  "devDependencies": {
    "wrangler": "^4.17.0"
  }
}
